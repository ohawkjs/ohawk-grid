import { OhGGridHeaderStylesPipe } from './oh-g-grid-header-styles.pipe';

describe('OhGGridHeaderStylesPipe', () => {
  it('create an instance', () => {
    const pipe = new OhGGridHeaderStylesPipe();
    expect(pipe).toBeTruthy();
  });
});
